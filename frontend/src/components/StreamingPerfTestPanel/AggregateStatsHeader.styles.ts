import type { CSSProperties } from "react";

/** Static styles for AggregateStatsHeader component. */
export const styles: Record<string, CSSProperties> = {
  row: { marginTop: 8 },
  valueStyle: { fontSize: 20 },
};
