import type { CSSProperties } from "react";
import { FONT_XS } from "../../styles/shared";

/** Styles for TaskCancelButton component. */
export const styles: Record<string, CSSProperties> = {
  button: FONT_XS,
};
