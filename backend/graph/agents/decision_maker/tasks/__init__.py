"""Tasks sub-package for decision_maker node.

- :mod:`llm_infer` — stream the LLM decision prompt and parse JSON
- :mod:`db_insert` — persist a :class:`DecisionReport` to ``fin_strategies.reports``
"""
